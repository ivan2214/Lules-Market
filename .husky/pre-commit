#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running lint-staged..."
pnpm run lint-staged

echo "Security check (all deps)..."
pnpm audit --audit-level=high || {
  echo "❌ Vulnerabilidades detectadas. Arreglalas antes de commitear."
  exit 1
}
